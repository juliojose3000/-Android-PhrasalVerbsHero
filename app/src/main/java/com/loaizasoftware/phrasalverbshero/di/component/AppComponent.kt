package com.loaizasoftware.phrasalverbshero.di.component

import com.loaizasoftware.phrasalverbshero.presentation.ui.activities.MainActivity
import com.loaizasoftware.phrasalverbshero.di.module.AppModule
import dagger.Component
import javax.inject.Singleton

/*@Singleton
@Component(modules = [AppModule::class]) //A dagger component serves as a bridge between the @Inject-annotated classes and the modules
interface AppComponent {
    fun inject(mainActivity: MainActivity) //This function is used to inject dependencies into the MainActivity class
    //fun inject(viewModel: VerbViewModel)
}*/